# solidgate-connector

Rust client + webhook types for [SolidGate](https://solidgate.com/) — mirror of `billerix-connector`.

Scaffolding stage — API/webhook structures will be filled in per provider docs.
